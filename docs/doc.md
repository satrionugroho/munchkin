# Usage

This documents shows us how to cold-start the application.

## Insert an asset
Just run the seeds inside `priv/seeds/seeds.exs` after that run `priv/seeds/company_seeds.exs`

## User wise
Run the `api.http` inside the `docs` folder
